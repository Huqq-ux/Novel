<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novel.reading.mapper.BookmarkMapper">
    <select id="selectByUserIdAndBookId" resultType="com.novel.common.entity.Bookmark">
        SELECT * FROM bookmarks WHERE user_id = #{userId} AND book_id = #{bookId} ORDER BY create_time DESC
    </select>
    <select id="selectByUserAndChapter" resultType="com.novel.common.entity.Bookmark">
        SELECT * FROM bookmarks WHERE user_id = #{userId} AND book_id = #{bookId} AND chapter_id = #{chapterId}
    </select>
</mapper>
